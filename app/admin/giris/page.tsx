'use client';
import {useState} from 'react';import {createClient} from '@supabase/supabase-js';import {ArrowRight,Eye,EyeOff,LockKeyhole} from 'lucide-react';import '../admin.css';
const sb=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);
export default function AdminLogin(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);
const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');const{data,error}=await sb.auth.signInWithPassword({email,password});setBusy(false);if(error)return setError(error.message==='Invalid login credentials'?'E-posta veya şifre hatalı.':`Giriş yapılamadı: ${error.message}`);if(!data.session)return setError('Oturum oluşturulamadı. Lütfen tekrar deneyin.');location.href='/admin'};
return <main className="admin-login"><section className="login-brand"><img src="/logo.png" alt="Honey Collection"/><p>YÖNETİM MERKEZİ</p><h1>Zarafetin<br/><em>ardındaki düzen.</em></h1><small>Randevular, ürünler ve içerikler tek güvenli merkezde.</small></section><section className="login-panel"><form onSubmit={submit}><div className="lock"><LockKeyhole/></div><p className="admin-kicker">YETKİLİ ERİŞİMİ</p><h2>Tekrar hoş geldiniz.</h2><p>Devam etmek için yönetici hesabınızla giriş yapın.</p><label>E-posta<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Şifre<div className="password"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)} aria-label="Şifreyi göster">{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="login-submit" disabled={busy}>{busy?'Kontrol ediliyor…':'Güvenli giriş'}<ArrowRight/></button><a href="/">← Siteye dön</a></form></section></main>}
